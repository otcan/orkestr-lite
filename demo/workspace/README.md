# Orkestr Lite demo workspace

This tiny repository contains one bounded implementation bug and a deterministic Node.js test suite with no network dependency.

Recommended mission:

> Find the failing test, identify the cause, implement the smallest correct fix, run the tests, and explain the change.

Run the tests with `npm test`.
