# v0.2 demo narration

Target: about 135 seconds. Speak naturally; do not speed up to hit the marks.
The editor holds each authentic capture for the same fraction of the finished
recording and labels every cut. Keep the final audio below 179 seconds.
For the unattended submission draft, a synthetic reading is permitted only when
the rendered video labels it as synthetic narration and records that choice in
`demo-metadata.json`.

| Time      | Narration                                                                                                                                                                                                                                                              |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0:00–0:18 | Orkestr Lite is an operational runtime around Codex: one local, persistent workstation controlled from the browser. Setup connects the user's eligible Codex account and an optional WhatsApp linked device. Nothing is hosted, and only a loopback port is published. |
| 0:18–0:39 | The product keeps one visible conversation across browser messages, schedules, and WhatsApp. This run uses GPT-5.6 with medium effort. YOLO means Codex has full access inside the isolated Ubuntu workstation, not to the host or Docker socket.                      |
| 0:39–0:59 | I asked Codex to compare official OpenHands, Open Interpreter, and goose runtime documentation. This is the real Ubuntu Desk and Chromium session used for the research. The visible jump cut skips waiting time; it does not replace execution.                       |
| 0:59–1:17 | Codex wrote cited Markdown and self-contained HTML into the shared workspace. Files can browse the workstation, preview or download the artifacts, and explicitly send an allowed file through the linked self-chat.                                                   |
| 1:17–1:37 | From WhatsApp I requested a sourced recommendation for a solo operator. Orkestr accepted the self-chat message, kept it in the same durable queue, and returned the updated Markdown report as a document. Personal identity is redacted in this capture.              |
| 1:37–1:55 | Schedules are operational inputs too. The weekly runtime watch previews its future runs, rejects overlaps, and its Run now result enters the same conversation instead of creating another thread.                                                                     |
| 1:55–2:15 | The completed report preserves source links and distinguishes “not documented in the reviewed sources” from “unsupported.” Orkestr records requested and effective GPT-5.6 provenance, queue and recovery events, files, and context state around Codex's work.        |
| 2:15–2:25 | This is local, single-user infrastructure—not a replacement for Codex. The repository, paired control and Desk images, release evidence, and exact judge commands are public and reproducible.                                                                         |
